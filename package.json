{
  "name": "@vercel-labs/xref",
  "version": "0.6.0",
  "private": true,
  "description": "Map the complete reference graph around GitHub issues, pull requests, and repository backlogs.",
  "packageManager": "bun@1.3.14",
  "type": "module",
  "bin": {
    "xref": "./src/cli.ts"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./transport/shell": {
      "types": "./dist/transports/shell.d.ts",
      "import": "./dist/transports/shell.js"
    },
    "./transport/http": {
      "types": "./dist/transports/http.d.ts",
      "import": "./dist/transports/http.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "start": "bun run src/cli.ts",
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "format": "biome format --write src",
    "check": "bun run lint && bun run typecheck && bun test && bun run build"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist",
    "src",
    "skills",
    "!src/**/*.test.ts",
    "!dist/**/*.map"
  ],
  "keywords": [
    "github",
    "cli",
    "pull-request",
    "issue",
    "reference-graph",
    "agent",
    "triage",
    "software-factory"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vercel-labs/xref.git"
  },
  "homepage": "https://github.com/vercel-labs/xref#readme",
  "bugs": {
    "url": "https://github.com/vercel-labs/xref/issues"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/bun": "latest",
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0"
  }
}
