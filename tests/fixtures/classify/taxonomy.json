{
  "schemaVersion": 1,
  "repo": "sample/public-repo",
  "version": "synthetic-1",
  "components": [
    {
      "id": "windows",
      "description": "Creating, closing and managing application windows.",
      "examples": ["A window stays open after closing it."]
    },
    { "id": "settings", "description": "Saving and restoring user settings." }
  ]
}
